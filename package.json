{
  "name": "skill_for_web_service",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.5.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
  },
  "devDependencies": {
    "turbo": "^2.0.9"
  }
}
